---
import logoMinistere from './_assets/logo_ministre.png';
import logoAlgerieTelecom from './_assets/logo_Algérie_Télécom.svg';
import logoMobilis from './_assets/logo_Mobilis.svg';
import videoSrc from './_assets/video.mp4';
import '../../styles/global.css';
import './_styles/presentation.css';
---

<!doctype html>
<html lang="fr" class="w-full h-full overflow-hidden">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Digital Productivity: Work Smarter, Not Harder • 7.77 Program</title>
    <meta name="description" content="Digital Productivity: Work Smarter, Not Harder by Abderrahmane SAOUDI - 777 Program (Age: 24-30)." />
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="Digital Productivity: Work Smarter, Not Harder • 7.77 Program" />
    <meta property="og:description" content="Presentation by Abderrahmane SAOUDI for the 777 Program." />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="fr_DZ" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body class="bg-[#e6f7f1] text-slate-800 font-sans w-screen h-screen overflow-hidden flex items-center justify-center relative bg-particles antialiased select-none">
    <!-- 1. Background Ambient Multi-Aurora Morphing Blobs -->
    <div class="bg-aurora-container">
      <div class="aurora-blob aurora-blob-1"></div>
      <div class="aurora-blob aurora-blob-2"></div>
      <div class="aurora-blob aurora-blob-3"></div>
      <div class="aurora-blob aurora-blob-4"></div>
    </div>

    <!-- 2. Twinkling Keynote Sparkle Stars -->
    <svg class="sparkle-star sparkle-1 w-5 h-5 text-emerald-800/40" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 0L14.5 9.5L24 12L14.5 14.5L12 24L9.5 14.5L0 12L9.5 9.5z" />
    </svg>
    <svg class="sparkle-star sparkle-2 w-4 h-4 text-sky-800/35" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 0L14.5 9.5L24 12L14.5 14.5L12 24L9.5 14.5L0 12L9.5 9.5z" />
    </svg>
    <svg class="sparkle-star sparkle-3 w-4 h-4 text-amber-800/35" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 0L14.5 9.5L24 12L14.5 14.5L12 24L9.5 14.5L0 12L9.5 9.5z" />
    </svg>
    <svg class="sparkle-star sparkle-4 w-5 h-5 text-emerald-800/35" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 0L14.5 9.5L24 12L14.5 14.5L12 24L9.5 14.5L0 12L9.5 9.5z" />
    </svg>

    <!-- 3. Floating Background Geometric Tech Nodes -->
    <svg class="floating-node node-1 w-6 h-6 text-emerald-800/40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 2v20M2 12h20" />
    </svg>
    <svg class="floating-node node-2 w-5 h-5 text-sky-800/35" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polygon points="12 2 22 12 12 22 2 12" />
    </svg>
    <svg class="floating-node node-3 w-5 h-5 text-emerald-800/35" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="12" cy="12" r="8" />
    </svg>
    <svg class="floating-node node-4 w-6 h-6 text-amber-800/30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 2v20M2 12h20" />
    </svg>

    <!-- 4. Main Presentation Card (94% screen view, 16:9 ratio, bordered, levitating, 100% white background) -->
    <main 
      id="presentationDeck" 
      data-current-slide="1" 
      class="presentation-frame frame-border-pulse bg-white rounded-3xl sm:rounded-[32px] border-2 border-emerald-800/25 relative flex flex-col justify-between p-6 sm:p-10 md:p-12 lg:p-14 xl:p-16 overflow-hidden z-10 hover:border-emerald-800/50 transition-colors duration-500"
    >
      <!-- Architectural Corner L-Brackets -->
      <div class="corner-bracket corner-tl"></div>
      <div class="corner-bracket corner-tr"></div>
      <div class="corner-bracket corner-bl"></div>
      <div class="corner-bracket corner-br"></div>

      <div class="content-layer relative z-10 h-full flex flex-col justify-between">
        <!-- Top: Dynamic Header Pills -->
        <div class="pt-1 relative min-h-[46px]">
          <!-- Slide 1 Top: Speaker Pill -->
          <div class="top-pill-1 flex items-center">
            <div class="speaker-pill relative overflow-hidden inline-flex items-center gap-2.5 bg-[#1E824C] hover:bg-[#006233] text-white font-bold text-sm sm:text-base md:text-lg px-5 sm:px-7 py-2 sm:py-2.5 rounded-full tracking-tight transition-transform duration-200 hover:-translate-y-0.5 cursor-default select-none">
              <span class="relative flex h-2.5 w-2.5">
                <span class="live-dot-pulse absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-emerald-300"></span>
              </span>
              <span>Abderrahmane SAOUDI</span>
            </div>
          </div>

          <!-- Slide 2 Top: 7.77 Program Badge in top left (no Age) -->
          <div class="top-pill-2 absolute inset-0 flex items-center">
            <div class="inline-flex items-center gap-2.5 bg-[#1E824C] hover:bg-[#006233] text-white font-bold text-sm sm:text-base md:text-lg px-5 sm:px-7 py-2 sm:py-2.5 rounded-full tracking-tight transition-transform duration-200 hover:-translate-y-0.5 cursor-default select-none">
              <span class="w-2.5 h-2.5 rounded-full bg-emerald-300"></span>
              <span>7.77 Program</span>
            </div>
          </div>
        </div>

        <!-- Middle: Dynamic Slide Panes Area -->
        <div class="relative w-full my-auto py-2 min-h-[320px] sm:min-h-[360px] md:min-h-[400px] lg:min-h-[440px] flex items-center">
          
          <!-- ================= SLIDE 1 PANE ================= -->
          <section id="slide-1-content" class="slide-pane slide-pane-1 w-full flex flex-col md:flex-row items-center justify-between gap-6 md:gap-8 lg:gap-8 xl:gap-10">
            <!-- Title Left Block -->
            <div class="flex flex-col gap-1 sm:gap-2 shrink-0">
              <!-- First Line: Green & Larger with Dynamic Shimmer Gradient -->
              <h1 class="title-highlight font-black text-3xl sm:text-4xl md:text-5xl lg:text-[3.8rem] xl:text-[4.4rem] tracking-tight leading-[1.08] whitespace-nowrap">
                Digital Productivity
              </h1>
              <!-- Second Line: Dark & Complementary -->
              <h2 class="title-subtext font-light text-xl sm:text-2xl md:text-3xl lg:text-3xl xl:text-4xl text-slate-900 tracking-tight leading-tight">
                Work Smarter, Not Harder
              </h2>
            </div>

            <!-- Video Right Block (Bigger, borderless, pure transparent fade loop) -->
            <div class="video-wrapper flex-1 w-full max-w-[420px] sm:max-w-[540px] md:max-w-[660px] lg:max-w-[780px] xl:max-w-[880px] 2xl:max-w-[940px] flex justify-center md:justify-end">
              <div class="relative w-full aspect-video rounded-3xl overflow-hidden bg-transparent">
                <video 
                  id="heroVideo"
                  src={videoSrc}
                  autoplay
                  muted
                  playsinline
                  class="hero-video w-full h-full object-contain md:object-cover bg-transparent pointer-events-none opacity-0"
                ></video>
              </div>
            </div>
          </section>

          <!-- ================= SLIDE 2 PANE: SPEAKER BIO ================= -->
          <section id="slide-2-content" class="slide-pane slide-pane-2 w-full flex flex-col gap-5 sm:gap-6 lg:gap-8">
            <!-- Speaker Header -->
            <div class="flex flex-col gap-1 sm:gap-2">
              <div class="inline-flex items-center gap-2 text-xs sm:text-sm font-bold uppercase tracking-wider text-[#006233]">
                <span class="w-2 h-2 rounded-full bg-[#1E824C]"></span>
                <span>Intervenant • Speaker</span>
              </div>
              <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-[3.4rem] font-black text-slate-900 tracking-tight leading-tight">
                Abderrahmane <span class="title-highlight">SAOUDI</span>
              </h2>
            </div>

            <!-- 3 Bio Pillars Grid -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 sm:gap-5 lg:gap-6 w-full">
              <!-- Pillar 1: Graphic Designer and Advisor -->
              <div class="flex flex-col justify-between p-5 sm:p-6 rounded-2xl sm:rounded-3xl bg-emerald-50/50 border border-emerald-800/20 transition-all duration-300 hover:-translate-y-1 hover:border-emerald-700/40">
                <div class="w-12 h-12 rounded-2xl bg-[#1E824C] text-white flex items-center justify-center mb-4">
                  <svg class="w-6 h-6 fill-none stroke-current stroke-[2]" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                  </svg>
                </div>
                <div>
                  <h3 class="text-base sm:text-lg font-bold text-slate-900 tracking-tight leading-snug">
                    Graphic Designer and Advisor
                  </h3>
                  <p class="text-xs sm:text-sm text-slate-600 mt-2 leading-relaxed font-normal">
                    Conception d'identités visuelles, direction artistique, stratégie de design de marque et expérience utilisateur (UI/UX).
                  </p>
                </div>
              </div>

              <!-- Pillar 2: Mobile and Frontend Web Developer -->
              <div class="flex flex-col justify-between p-5 sm:p-6 rounded-2xl sm:rounded-3xl bg-sky-50/50 border border-sky-800/20 transition-all duration-300 hover:-translate-y-1 hover:border-sky-700/40">
                <div class="w-12 h-12 rounded-2xl bg-[#0080C8] text-white flex items-center justify-center mb-4">
                  <svg class="w-6 h-6 fill-none stroke-current stroke-[2]" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
                  </svg>
                </div>
                <div>
                  <h3 class="text-base sm:text-lg font-bold text-slate-900 tracking-tight leading-snug">
                    Mobile and Frontend Web Developer
                  </h3>
                  <p class="text-xs sm:text-sm text-slate-600 mt-2 leading-relaxed font-normal">
                    Développement d'applications mobiles cross-platform (Flutter) et d'architectures web SSR modernes et ultra-légères.
                  </p>
                </div>
              </div>

              <!-- Pillar 3: Former GDG Ghardaia Club President -->
              <div class="flex flex-col justify-between p-5 sm:p-6 rounded-2xl sm:rounded-3xl bg-amber-50/50 border border-amber-800/20 transition-all duration-300 hover:-translate-y-1 hover:border-amber-700/40">
                <div class="w-12 h-12 rounded-2xl bg-[#D97706] text-white flex items-center justify-center mb-4">
                  <svg class="w-6 h-6 fill-none stroke-current stroke-[2]" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
                  </svg>
                </div>
                <div>
                  <h3 class="text-base sm:text-lg font-bold text-slate-900 tracking-tight leading-snug">
                    Former GDG Ghardaia Club President
                  </h3>
                  <p class="text-xs sm:text-sm text-slate-600 mt-2 leading-relaxed font-normal">
                    Leadership communautaire Google Developers Group, organisation d'ateliers tech, hackathons et mentorat numérique.
                  </p>
                </div>
              </div>
            </div>
          </section>

        </div>

        <!-- Bottom: Dividing Line + Next/Previous Navigation Buttons + Responsive Footer -->
        <div class="pb-1">
          <!-- Horizontal Line with Previous on Left and Next on Right -->
          <div class="relative w-full flex items-center my-2 sm:my-3">
            <div class="divider-line relative w-full h-0.5 bg-slate-900 overflow-hidden"></div>

            <!-- Previous Button on the LEFT side of the H line -->
            <div class="prev-btn-wrapper absolute left-0 flex items-center justify-center z-20">
              <button 
                id="prevSlideBtn" 
                type="button" 
                aria-label="Previous Slide" 
                class="w-10 h-10 sm:w-12 sm:h-12 rounded-full bg-slate-100 hover:bg-emerald-50 text-slate-800 hover:text-[#1E824C] border-2 border-slate-300 hover:border-emerald-600/50 flex items-center justify-center transition-all duration-300 hover:scale-110 active:scale-95 cursor-pointer select-none" 
                title="Slide Précédente"
              >
                <svg class="w-4 h-4 mr-0.5 fill-none stroke-current stroke-[2.5]" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
                </svg>
              </button>
            </div>

            <!-- Next / Play Button on the RIGHT side of the H line -->
            <div class="next-btn-wrapper absolute right-0 flex items-center justify-center z-20">
              <!-- Spinning Dashed Orbit Ring -->
              <div class="orbit-ring absolute -inset-6 rounded-full border border-dashed border-emerald-600/30 pointer-events-none"></div>
              <div class="sonar-wave absolute -inset-1.5 rounded-full border-2 border-emerald-600/45 pointer-events-none"></div>
              <div class="sonar-wave-2 absolute -inset-3.5 rounded-full border-[1.5px] border-sky-600/35 pointer-events-none"></div>
              <div class="sonar-wave-3 absolute -inset-5.5 rounded-full border border-emerald-500/25 pointer-events-none"></div>
              <button 
                id="nextSlideBtn" 
                type="button" 
                aria-label="Next Slide" 
                class="w-10 h-10 sm:w-12 sm:h-12 rounded-full bg-[#1E824C] hover:bg-[#006233] flex items-center justify-center text-white border-2 border-white transition-all duration-300 hover:scale-115 hover:rotate-6 cursor-pointer select-none" 
                title="Slide Suivante"
              >
                <svg class="play-icon w-4 h-4 ml-0.5 fill-current" viewBox="0 0 24 24">
                  <path d="M8 5v14l11-7z" />
                </svg>
              </button>
            </div>
          </div>

          <!-- Bottom Row: Left Logos (scales smaller into corner) & Right Title on Slide 2 -->
          <div class="flex items-center justify-between gap-4 mt-2 sm:mt-4 min-h-[48px] sm:min-h-[56px]">
            <!-- Left: Program Tag & Institutional Logos -->
            <div class="flex items-center gap-4 sm:gap-6">
              <!-- Program & Age Tag (visible on slide 1, collapses on slide 2) -->
              <div class="footer-program-age-tag program-tag text-[#1E824C] font-extrabold text-base sm:text-xl md:text-2xl tracking-tight inline-flex items-center gap-2 shrink-0">
                <span class="w-2 h-2 rounded-full bg-emerald-600"></span>
                <span>7.77 Program | Age: 24-30</span>
              </div>

              <!-- Institutional Logos Group (shrinks into left bottom corner on slide 2) -->
              <div class="footer-logos-container flex items-center gap-4 sm:gap-7 md:gap-9">
                <!-- Ministère de la Poste et des Télécommunications -->
                <div class="logo-wrapper-1 flex items-center transition-transform duration-300 hover:-translate-y-2 hover:scale-105">
                  <img 
                    src={logoMinistere.src} 
                    alt="Ministère de la Poste et des Télécommunications" 
                    class="h-10 sm:h-14 md:h-16 w-auto object-contain"
                  />
                </div>

                <!-- Algérie Télécom -->
                <div class="logo-wrapper-2 flex items-center transition-transform duration-300 hover:-translate-y-2 hover:scale-105">
                  <img 
                    src={logoAlgerieTelecom.src} 
                    alt="Algérie Télécom" 
                    class="h-7 sm:h-10 md:h-12 w-auto object-contain"
                  />
                </div>

                <!-- Mobilis -->
                <div class="logo-wrapper-3 flex items-center transition-transform duration-300 hover:-translate-y-2 hover:scale-105">
                  <img 
                    src={logoMobilis.src} 
                    alt="Mobilis" 
                    class="h-6 sm:h-8 md:h-9 w-auto object-contain"
                  />
                </div>
              </div>
            </div>

            <!-- Right: Title "Digital Productivity: Work Smarter, Not Harder" on Slide 2 -->
            <div class="footer-slide2-title text-right shrink-0">
              <div class="title-highlight font-black text-sm sm:text-base md:text-lg lg:text-xl tracking-tight leading-tight">
                Digital Productivity
              </div>
              <div class="text-xs sm:text-sm font-light text-slate-700 tracking-tight leading-tight">
                Work Smarter, Not Harder
              </div>
            </div>
          </div>

        </div>
      </div>
    </main>

    <!-- Synchronized Video & Slide Deck Controller -->
    <script is:inline>
      (() => {
        const deck = document.getElementById('presentationDeck');
        const prevBtn = document.getElementById('prevSlideBtn');
        const nextBtn = document.getElementById('nextSlideBtn');
        const video = document.getElementById('heroVideo');

        let currentSlide = 1;
        const totalSlides = 2;
        let isFadingOut = false;

        // Video Synchronized Fade Controller
        const fadeInVideo = () => {
          if (!video) return;
          isFadingOut = false;
          video.style.opacity = '1';
        };

        if (video) {
          video.addEventListener('play', fadeInVideo);
          video.addEventListener('playing', fadeInVideo);

          video.addEventListener('timeupdate', () => {
            if (!video.duration || isFadingOut || currentSlide !== 1) return;
            const timeLeft = video.duration - video.currentTime;
            // Smooth fadeout over final 1.2s of video
            if (timeLeft <= 1.2) {
              isFadingOut = true;
              video.style.opacity = '0';
            }
          });

          video.addEventListener('ended', () => {
            if (currentSlide !== 1) return;
            video.style.opacity = '0';
            setTimeout(() => {
              if (currentSlide !== 1) return;
              video.currentTime = 0;
              isFadingOut = false;
              video.play().then(fadeInVideo).catch(() => {});
            }, 350);
          });

          // Autoplay on slide 1 load
          if (!video.paused && video.currentTime > 0) {
            fadeInVideo();
          } else {
            video.play().then(fadeInVideo).catch(() => {});
          }
        }

        // Slide Deck Navigation
        function setSlide(num) {
          if (num < 1 || num > totalSlides) return;
          currentSlide = num;
          if (deck) deck.setAttribute('data-current-slide', String(currentSlide));
          
          if (nextBtn) {
            if (currentSlide === totalSlides) {
              nextBtn.classList.add('opacity-40', 'cursor-not-allowed');
            } else {
              nextBtn.classList.remove('opacity-40', 'cursor-not-allowed');
            }
          }

          // Video state management between slides
          if (video) {
            if (currentSlide === 1) {
              video.currentTime = 0;
              isFadingOut = false;
              video.play().then(fadeInVideo).catch(() => {});
            } else {
              video.pause();
            }
          }
        }

        if (prevBtn) {
          prevBtn.addEventListener('click', () => setSlide(currentSlide - 1));
        }

        if (nextBtn) {
          nextBtn.addEventListener('click', () => {
            if (currentSlide < totalSlides) {
              setSlide(currentSlide + 1);
            }
          });
        }

        // Keyboard Navigation (Arrow Keys & Space)
        window.addEventListener('keydown', (e) => {
          if (e.key === 'ArrowRight' || e.key === ' ' || e.key === 'Enter') {
            if (currentSlide < totalSlides) {
              e.preventDefault();
              setSlide(currentSlide + 1);
            }
          } else if (e.key === 'ArrowLeft' || e.key === 'Backspace') {
            if (currentSlide > 1) {
              e.preventDefault();
              setSlide(currentSlide - 1);
            }
          }
        });
      })();
    </script>
  </body>
</html>
