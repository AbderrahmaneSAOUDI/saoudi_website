---
/**
 * Legacy forwarder: AdminTodoCard -> AdminTasksCard
 */
import AdminTasksCard from './AdminTasksCard.astro';

interface Props {
	initialTodos?: any[];
	initialTasks?: any[];
	isPrimaryAdmin?: boolean;
}

const { initialTodos, initialTasks, isPrimaryAdmin } = Astro.props;
---

<AdminTasksCard initialTasks={initialTasks ?? initialTodos} isPrimaryAdmin={isPrimaryAdmin} />
